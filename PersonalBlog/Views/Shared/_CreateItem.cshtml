@model string;

@if (User.Identity.IsAuthenticated)
{
    <div style="position:fixed; right:25px; bottom:25px; z-index:999" class="text-white">
        <a asp-controller="@Model" asp-action="Create" class="btn btn-secondary">Create Post</a>
    </div>
}